import { FilterRatingsPipe } from './filter-ratings.pipe';

describe('FilterRatingsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRatingsPipe();
    expect(pipe).toBeTruthy();
  });
});
